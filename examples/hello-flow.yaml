name: hello-flow
description: Example flow that prints greetings
steps:
  - id: greet
    type: command
    run: echo "Hello from flow"
  - id: sum
    type: command
    run: node -e "console.log('1 + 1 =', 1 + 1)"
