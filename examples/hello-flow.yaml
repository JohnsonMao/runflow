name: hello-flow
description: Example flow that prints greetings
steps:
  - id: greet
    type: set
    set: { message: "Hello from flow" }
    dependsOn: []
  - id: sum
    type: set
    set: { result: 2 }
    dependsOn: [greet]
