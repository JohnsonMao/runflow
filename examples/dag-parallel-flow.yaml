# Parallel branches: b and c both depend on a.
# Run: flow run dag-parallel-flow.yaml
name: dag-parallel
description: Parallel branches after root
steps:
  - id: a
    type: set
    set: { root: true }
    depends-on: []
  - id: b
    type: set
    set: { branch: "B" }
    depends-on: [a]
  - id: c
    type: set
    set: { branch: "C" }
    depends-on: [a]
