name: set-flow
description: Simple set steps (no JS file step)
steps:
  - id: first
    type: set
    set: { fromExample: true, message: "from set step" }
    dependsOn: []
  - id: second
    type: set
    set: { combined: "{{ first.fromExample }}" }
    dependsOn: [first]
